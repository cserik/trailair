# Improvement backlog

Well-scoped items the improve loop (or any contributor) may pick up.
One line each: what + how success is measured.

- (example) Tighten skills/feature.md without losing steps — metric: skills_doc_lines
- (example) Add a bootstrap heuristic for monorepos — gate: bootstrap works on a fixture monorepo
